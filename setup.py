#!/usr/bin/env python

from distutils.core import setup
import setuptools

setup(name='hipp',
      version='0.1',
      description='Functions to pre-process scanned historical images.',
      author='Friedrich Knuth',
      author_email='knuth@uw.edu',
      # url='',
      packages=setuptools.find_packages(),
      # install_requires=[''],
     )
